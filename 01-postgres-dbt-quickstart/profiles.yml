postgres_demo:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: dbt_user
      password: dbt_password
      port: 5432
      dbname: dbt_demo
      schema: public
      threads: 4
      keepalives_idle: 0
      search_path: public 